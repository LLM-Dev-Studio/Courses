---
lessonType: quiz
passThreshold: 80
maxAttempts: 2
resetScopeOnFail: module
questions:
  - prompt: "What is the primary purpose of AI Dev Studio?"
    options:
      - "To coordinate multiple specialised AI agents to execute software development tasks"
      - "To provide a single AI chat interface for writing code"
      - "To manage version control and CI/CD pipelines"
      - "To replace the need for human developers entirely"
    answer: "To coordinate multiple specialised AI agents to execute software development tasks"
    explanation: "AI Dev Studio orchestrates a team of specialised agents (Developer, Architect, QA, etc.) to tackle complex tasks collaboratively, rather than relying on a single AI model."

  - prompt: "What is an Executor in AI Dev Studio?"
    options:
      - "The pluggable LLM backend that an agent uses to run"
      - "A special agent that oversees other agents"
      - "The process that assigns tasks to agents"
      - "A type of decision raised by an agent"
    answer: "The pluggable LLM backend that an agent uses to run"
    explanation: "An Executor is the swappable LLM backend (Anthropic, Ollama, LM Studio, etc.) configured per agent. The ModelResolver selects the right executor at runtime."

  - prompt: "What happens when an agent raises a Decision?"
    options:
      - "The agent pauses and waits for a human (or another agent) to resolve it before continuing"
      - "The task is automatically moved to the Done column"
      - "The agent switches to a different executor and retries"
      - "The session is terminated and a new one starts"
    answer: "The agent pauses and waits for a human (or another agent) to resolve it before continuing"
    explanation: "A Decision is a typed question raised when an agent gets stuck. It blocks further progress until resolved. Resolving sends a Message back to the agent's inbox."

  - prompt: "Why is the class in code called BoardTask rather than Task?"
    options:
      - "To avoid a naming collision with .NET's System.Threading.Tasks.Task"
      - "Because it represents a different concept from a planning task"
      - "Because the board was added after the initial design"
      - "To match the naming convention of the WinUI framework"
    answer: "To avoid a naming collision with .NET's System.Threading.Tasks.Task"
    explanation: "The domain term is always 'Task' in UI and documentation. The class name BoardTask avoids conflicts with the .NET Task type used for async operations."

  - prompt: "Which layer in the architecture do BOTH the WinUI app and the VS Code extension ultimately depend on?"
    options:
      - "ai-dev.core"
      - "ai-dev.api"
      - "ai-dev.mcp"
      - "ai-dev-net.AppHost"
    answer: "ai-dev.core"
    explanation: "All domain logic lives in ai-dev.core. The WinUI app accesses it in-process via DI, while the VS Code extension accesses it over the HTTP/SignalR layer provided by ai-dev.api — but both surfaces use the same core services."

  - prompt: "What is a Planning Session?"
    options:
      - "A structured multi-turn LLM conversation that produces a plan artefact before execution begins"
      - "The first session run by a new agent after it is created"
      - "A session where the Architect agent plans the system design"
      - "A daily standup summary generated by the Overwatch agent"
    answer: "A structured multi-turn LLM conversation that produces a plan artefact before execution begins"
    explanation: "A Planning Session is distinct from an Agent Session. It runs a multi-turn conversation to decompose goals into tasks and constraints before any execution work starts."
---
